<persistence xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns="http://java.sun.com/xml/ns/persistence"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_2_0.xsd"
             version="2.0">

    <persistence-unit name="com.bayoumi.data.persistence">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entities auto-detected -->
        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <properties>
            <!-- JDBC connection -->
            <property name="jakarta.persistence.jdbc.driver" value="org.sqlite.JDBC"/>
            <!-- TODO:change this path later -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:sqlite:C:\Users\love_\IdeaProjects\Azkar-App\src\main\resources\db\data.db"/>

            <!-- Hibernate dialect -->
            <property name="hibernate.dialect"
                      value="org.hibernate.community.dialect.SQLiteDialect"/>

            <!-- Schema auto-generation -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

    <persistence-unit name="com.bayoumi.locations.persistence">

        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Entities auto-detected -->
        <exclude-unlisted-classes>false</exclude-unlisted-classes>

        <properties>
            <!-- JDBC connection -->
            <property name="jakarta.persistence.jdbc.driver" value="org.sqlite.JDBC"/>
            <!-- TODO:change this path later -->
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:sqlite:C:\Users\love_\IdeaProjects\Azkar-App\src\main\resources\db\locations.db"/>

            <!-- Hibernate dialect -->
            <property name="hibernate.dialect"
                      value="org.hibernate.community.dialect.SQLiteDialect"/>

            <!-- Schema auto-generation -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>


</persistence>